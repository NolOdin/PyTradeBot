Установи зависимости: pip install ccxt pandas scikit-learn requests python-telegram-bot.
Установи env-переменные: export BINANCE_API_KEY=... и т.д.
Запусти: python main.py.
Мониторь логи в bot.log и DB в trades.db (открой в SQLite viewer).
Тестируй: Сначала на тестовом API Binance (sandbox=True в ccxt).
Инструкции по запуску (обновлённые)

Зависимости: Те же — pip install ccxt pandas scikit-learn requests python-telegram-bot.
Env-переменные:
export BYBIT_API_KEY=your_key
export BYBIT_SECRET=your_secret
Остальные (DeepSeek, Telegram) — опционально.

Тестирование: В trade_engine.py sandbox=True по умолчанию. Создай тестовый аккаунт на Bybit Testnet. Для прод: sandbox=False.
Запуск:python main.py. Логи в bot.log, БД в trades.db.
Проверка: Запусти python -c "import ccxt; print(ccxt.bybit().fetch_ticker('ETH/USDT'))" — должен выдать тикер.
python main.py --backtest --days <- Запуск бэкфиста 

Фича,Как добавить
Walk-forward,"Разбить данные: 70% обучение, 30% тест"
Сохранение отчёта,stats.to_csv('backtest_report.csv')
Веб-дашборд,Flask + Plotly
Оптимизация параметров,"Grid Search по стоп-лосс, порогу AI"

python main.py --optimize <- оптимизация стратегии. 

